# DataAutomation

## 写给 LLM 的文件，严禁虚空解释

skill、提示词、宪法、领域文件，凡是会被模型读到的，只写模型完成任务需要的信息。

**每一句话必须属于以下四类之一：**

1. 让它做什么
2. 禁止它做什么
3. 供它使用的素材（词表、场景种子、格式定义）
4. 正误对照的具体例子

**不属于以上四类的一律删掉，包括：**

- 解释规则为什么存在（「因为这是最致命的」「不然后面全废」）
- 和历史版本对比（「老产线是这样，现在是那样」）
- 对文档自身的评论（「这一节最厚」「放在最前面因为」）
- 项目层面的信息（配比百分比、这块占多少、产品定位）
- 动机和铺垫（「先把这件事想清楚」）

模型不需要知道项目背景、迭代历史、设计取舍。它需要知道现在要产出什么、按什么规矩产出。

## 判断方法

删掉一句话，模型的产出会变吗。不变就删。

## 例子

| 删 | 留 |
|---|---|
| 场景不具体，对话必然空洞。这是上游关卡，卡住了后面全废。 | 场景卡必须包含具体的时间、数量或原话。 |
| 这是老产线积累最厚的一块，区别在于信息密度要降下来。 | 一轮说一件事，说完等对方反应。 |
| 会说不知道是活人感里最值钱的一条，模型天然不爱这么干。 | 不知道就说不知道，每次换一种说法。 |
| 这个领域最容易写出 AI 味。 | （删，后面的具体禁令自己会说话） |

## 适用范围

`data-generation/` 下所有文件。

`README.md` 和本文件是写给人看的，不受此约束。
