# Agent Arena - 产品蓝图

---

## 定位

让OC创作者把自己的角色"养活"——不只是立绘和设定文档，而是一个能说话、有性格、会成长的AI角色。

**目标人群：** OC创作者、绘圈、角色扮演爱好者、Live2D创作者

**核心卖点：** 你画的角色，现在能跟你说话了。而且越聊越像你设定的那个"人"。

**关键词：** 客制化、定制感、专属感

**为什么不用大厂：** 大厂模型随时可能下架或改版（GPT-4o下架风波），你花几个月调教的角色一夜归零。我们自建模型 + per-character LoRA，角色资产完全在平台可控，不受上游模型变更影响。

---

## V1：角色塑造 + 主动陪伴

### 核心循环

```
用户创建角色（名字、性格、口癖、背景故事、关系设定）
  → 平台用默认基座 + 角色设定生成初始人格（prompt + few-shot）
  → 用户和角色对话
  → 对回复👍👎 / 手动改写（"他不会这么说话"）
  → 👍 = positive sample，👎+改写 = DPO偏好对
  → 角色越来越接近用户心中的那个"人"
```

### 默认活人感

开箱即用的基座模型，不需要任何训练就具备：
- 不机器味，不说"作为AI我无法"
- 有情绪波动，会生气、会撒娇、会冷淡
- 记得之前聊过的事
- 口语化，有语气词，有停顿
- 会主动转话题，不只是被动回应

实现：用高质量角色对话数据预训一版默认LoRA，所有新角色共享这个底座。

### 角色设定系统

用户可配置：
- 基础信息：名字、年龄、性别、种族、职业
- 性格标签：傲娇、腹黑、温柔、毒舌、社恐……
- 口癖/说话风格：示例对话（用户直接写几段"他会怎么回"）
- 背景故事：自由文本
- 关系设定：和用户的关系（朋友、恋人、宿敌、师徒……）
- 世界观：现代、奇幻、赛博朋克、架空……

### 训练进化

用户和角色聊天积累数据后，可以选择训练：

**SFT（监督微调）：**
- 数据来源：用户改写的对话（"他应该这么说"）
- 效果：角色学会用户设定的说话方式和口癖

**DPO（偏好对齐）：**
- 数据来源：👍👎反馈
- 效果：角色学会什么该说什么不该说，什么语气对什么语气不对

**RLHF（后续完善）：**
- 更精细的奖励模型，处理复杂场景（角色在不同情境下的反应一致性）

训练不是强制的——不训练也能用（prompt + few-shot），训练是让角色"更像"的付费增值。

### 主动对话

角色不只是被动等你说话，会主动找你聊。

**触发机制：**
```
定时任务扫描记忆库 + 角色设定 + 时间上下文
  → 触发条件匹配
  → 生成符合角色人格的消息
  → 推送到用户（App通知 / 微信 / QQ）
```

**触发条件示例：**
- 时间触发："早上好"、"这么晚还没睡？"（结合角色性格——毒舌角色会说"又熬夜？活腻了？"）
- 记忆触发："你上次说周五要考试，怎么样了？"
- 关系触发："今天是我们认识第100天"（角色设定里的纪念日）
- 随机触发：角色"想到了什么"主动分享（基于世界观和性格生成）
- 缺席触发："三天没理我了，生我气了？"

**关键：主动消息的风格必须和角色人格一致。** 傲娇角色不会直接说"我想你了"，会说"才没有在等你消息呢"。这是靠LoRA训练保证的。

### IM平台接入

角色不只活在App里，可以出现在用户日常使用的通讯工具中：

| 平台 | 接入方式 | 能力 |
|------|----------|------|
| 微信 | 企业微信API / 微信机器人框架 | 文字对话 + 主动消息推送 |
| QQ | QQ机器人官方API | 文字对话 + 主动消息推送 |
| App内 | WebSocket | 全功能（对话 + Live2D + 语音） |

IM接入是纯文字模式，Live2D和语音只在App内支持。但核心价值在于：**角色就在你的聊天列表里，和真人朋友并排。**

### 待办/提醒

角色可以帮用户管理日常事务，但用角色自己的方式：

- 用户："帮我记一下明天下午三点开会"
- 角色（毒舌）："行吧，到时候叫你，你可别又迟到。"
- 第二天14:50推送："喂，还有十分钟开会，别说我没提醒你。"

实现：简单的todo list后端 + 定时任务，消息经过LLM用角色人格包装后推送。

### 记忆系统

LoRA管人格/风格，记忆系统管事实。两层分离。

```
对话交互
  → 自动提取事实存入向量库
  → 用户可在记忆面板审核、编辑、删除
  → 下次交互时检索相关记忆注入context
```

**向量库完全开放给用户：**
- 查看所有记忆条目
- 手动增删改任何一条
- 标记优先级（"永远记住" / "已过期"）
- 分类：角色设定、剧情事件、关系变化、世界观

### Live2D集成

用户可导入自己的Live2D模型，角色对话时模型联动：

| 状态 | Live2D表现 |
|------|------------|
| 普通说话 | 口型同步 + 轻微摇晃 |
| 开心 | 眯眼 + 微笑动作 |
| 生气 | 皱眉 + 身体后仰 |
| 害羞 | 低头 + 脸红图层 |
| 思考 | 歪头 + 眼神上移 |
| 惊讶 | 睁大眼 + 后退 |

实现：LLM输出附带情绪标签 → 前端映射到Live2D表情/动作参数

支持格式：Live2D Cubism SDK（.moc3 + .model3.json）

没有Live2D模型的用户，使用默认的参数化几何体（TARS风格）作为替代形象。

### 能力测试（Promptfoo）

每次训练后，自动跑测试集，让用户看到角色在"变真"。

**后端：** Promptfoo（MIT开源），定义测试用例 + 自动评分 + 生成报告。

**前端包装成用户能看懂的界面：**
- 雷达图：口癖还原度、性格一致性、情绪表达、记忆准确度等维度打分
- 前后对比："训练前 → 训练后"同一问题的回复对比
- 进度条："角色已还原 72% 的设定人格"
- 历史曲线：每次训练后分数变化趋势

**测试集来源：**
- 平台预设通用测试集（活人感、情绪表达）
- 用户设定自动生成测试用例（"以这个性格回应这个场景"）
- 用户历史纠正记录反向生成（"上次你改过的这种错，现在还会犯吗"）

### V1 功能清单

- [ ] Web App（Next.js）+ 移动端适配
- [ ] 角色创建/设定面板
- [ ] 对话界面 + 👍👎 + 改写
- [ ] 主动对话系统（定时扫描 + 触发条件 + 人格化消息）
- [ ] 微信/QQ机器人接入
- [ ] 待办/提醒功能（角色人格包装）
- [ ] Live2D模型导入 + 情绪联动
- [ ] 记忆面板（可查看/编辑/删除的向量库UI）
- [ ] 能力测试面板（雷达图 + 前后对比 + 成长曲线）
- [ ] 先用prompt + few-shot做角色个性化，验证体验后再接LoRA

### V1 验证点

1. OC创作者愿意花时间"调教"角色吗？
2. 默认活人感够不够让用户觉得"这不是普通chatbot"？
3. 训练后角色还原度提升，用户能感知到吗？
4. 主动搭话是提升粘性还是打扰用户？频率怎么控制？
5. 微信/QQ接入后，用户活跃度是否显著高于纯App？
6. Live2D联动是否显著提升沉浸感？

---

## V2：社交与展示

角色不只是自己玩——创作者想展示，想让别人也能和自己的角色互动。

### 角色广场

- 创作者可以把角色发布到广场，其他用户可以来聊
- 展示：Live2D形象 + 角色简介 + 创作者信息
- 排行榜：按互动量、好评率、收藏数排序
- 分类标签：性格、世界观、角色类型

### 角色分享/出租

- 创作者可以设置角色为"公开免费" / "付费体验"
- 付费模式：其他用户花X元/次或X元/月和这个角色聊天
- 收入分成：创作者70%，平台30%

### 接入LoRA训练

- [ ] 接入LLaMA-Factory SFT + DPO pipeline
- [ ] vLLM Multi-LoRA部署（或百炼API）
- [ ] V1积累的对话数据 → 真正训练per-character LoRA
- [ ] 增量训练自动触发（每100条新数据）
- [ ] RLHF pipeline（奖励模型训练 + PPO/DPO迭代）

---

## V3：语音 + 游戏互动

### 语音对话

角色不只能打字，还能说话。

| 环节 | 方案 |
|------|------|
| ASR | FunASR / Whisper |
| TTS | ChatTTS / GPT-SoVITS（可克隆音色） |
| 流式 | LLM吐字即合成，体感延迟<2秒 |

GPT-SoVITS支持少量样本音色克隆——用户上传几段角色"应该有的声音"，TTS就能用那个音色说话。

### 一起玩游戏

角色不只是聊天对象，可以和你一起玩游戏。

**静态Galgame模式：**
- 平台提供或用户上传剧本（分支叙事结构）
- 角色作为Galgame中的角色参与剧情
- 对话不是预设文本，而是LLM基于角色人格+剧情上下文实时生成
- 用户的选择影响剧情走向，角色有符合性格的反应
- 和传统Galgame的区别：对话是活的，不是读预设台词

**卡牌/策略游戏模式：**
- 角色作为对手或队友参与有明确规则的游戏
- 适合的游戏类型：回合制卡牌、棋类、文字策略
- LLM负责策略决策 + 角色人格化的对话（下棋时毒舌角色会嘲讽你）
- 实现：游戏逻辑用代码写死规则，LLM只负责决策和对话包装

### 硬件：ESP32 Wi-Fi 麦克风挂件

一个带Wi-Fi的麦克风，角色主题外壳。

**组成：**
- ESP32-S3（自带Wi-Fi）
- MEMS麦克风（INMP441）
- 锂电池（300mAh）
- 无扬声器，回复走用户自己的耳机

**BOM：¥20-30**

### 语音链路

```
ESP32挂件（连手机热点Wi-Fi）
  → 本地VAD检测到人声
  → 录音通过Wi-Fi直接发到云端API
  → 云端 ASR → LLM → TTS
  → WebSocket推送到手机App
  → App通过耳机播放
```

### iOS后台播放方案

App声明 `Audio, AirPlay, and Picture in Picture` 后台模式，维持AVAudioSession：

```
App启动 → 开启AVAudioSession(.playback) → 保持WebSocket连接
  → 云端推消息过来（角色回复 / 主动搭话）
  → App收到 → TTS播放 → 耳机出声
  → 播完继续待命，App不会被杀
```

---

## 训练架构

### 基座模型

- MVP：Qwen2.5-7B（中英双语，Apache 2.0）
- 后续：Qwen2.5-32B
- 默认LoRA：用高质量角色对话数据预训一版，开箱即用有活人感
- 角色个性化LoRA：在默认基础上，用用户纠正/偏好数据继续训

### 训练框架

| 框架 | 用途 |
|------|------|
| LLaMA-Factory | SFT + DPO + RLHF训练 |
| Unsloth | 加速训练，2x速度，70%显存节省 |

### 训练数据来源

| 来源 | 数据类型 |
|------|----------|
| 对话👍👎 | DPO偏好对 |
| 对话改写 | SFT对（模型回复→用户改写版） |
| 角色设定 | 系统prompt约束 |
| 记忆编辑 | 事实性约束 |
| 语音对话（V3） | 转文字后同上 |

### 推理

- MVP：百炼/Together AI 微调+推理API
- 后期：vLLM + S-LoRA 自建，单GPU服务200-500用户

### 记忆存储

- 向量库：Qdrant
- 结构化记忆：PostgreSQL
- 用户记忆面板：前端直接CRUD向量库条目

### 数据安全

- 种子扩写在服务端完成，用户只看到审核界面
- LoRA权重不提供下载，只提供API调用
- 用户通过平台交互，拿不到JSONL和权重文件

---

## 开源资源

| 数据集 | 规模 | 许可 | 用途 |
|--------|------|------|------|
| OpenCharacter | 30.6万条 | Apache 2.0 | 角色对话 |
| UltraFeedback | 6.4万条 | MIT | DPO偏好对 |
| WildChat-1M | 100万条 | ODC-BY | 真实用户对话 |

合成工具：Distilabel（Argilla），或自建pipeline用强模型生成角色对话数据。

---

## 成本模型

### 单角色

| 项目 | 成本 |
|------|------|
| 种子扩写 | ¥3-15 |
| 首次LoRA训练 | ¥2-7 |
| 增量训练 | ~¥0.3/次 |
| **总计首次** | **¥5-22** |

### 月运营（早期）

| 项目 | 月成本 |
|------|--------|
| 推理GPU/API | ¥1000-2000 |
| 数据库+存储 | ¥200-500 |
| 其他 | ¥300-500 |
| **总计** | **¥1500-3000** |

### 盈亏平衡

200付费用户 × ¥29/月 = ¥5,800 - ¥2,250成本 = ¥3,550毛利（61%）

---

## 付费设计

| 功能 | 免费 | Pro ¥29/月 |
|------|------|------------|
| 角色创建 | 1个角色 | 无限角色 |
| 基础对话 | 有限次数 | 无限 |
| 主动对话 | 不开放 | 开放（可调频率） |
| 微信/QQ接入 | 不开放 | 开放 |
| 待办/提醒 | 不开放 | 开放 |
| Live2D导入 | 不开放 | 开放 |
| 记忆系统 | 不开放 | 完全开放可编辑 |
| SFT/DPO训练 | 不开放 | 开放 |
| 能力测试 | 基础 | 完整报告 |
| 角色广场发布（V2） | 不开放 | 开放 |
| 语音对话（V3） | 不开放 | 开放 |
| 游戏互动（V3） | 不开放 | 开放 |

免费版体验角色对话的基本效果，付费版解锁训练、Live2D、主动陪伴、IM接入、游戏全套。

---

## 合规

| 层级 | 做法 |
|------|------|
| 输入侧 | 角色设定 + 训练数据过关键词 + 大模型审核，拦违规内容 |
| 训练侧 | LoRA训完跑安全测试集，不通过不上线 |
| 输出侧 | 推理时加轻量safety filter |

---

## TARS 参数化几何体（默认形象）

没有Live2D模型的用户使用默认几何体形象。

### 可定制维度

| 维度 | 选项 |
|------|------|
| 形态 | 长方体、多面体、球体、棱柱、不规则晶体 |
| 材质 | 金属、玻璃、磨砂、全息、液态 |
| 纹理 | 电路纹、几何线条、脉冲波、星图 |
| 发光色 | 自由选色，渐变，呼吸变色 |
| 比例 | 宽扁、细长、对称/不对称 |
| 附属件 | 悬浮粒子、光环、轨道环、能量场 |

### 状态动画

| 状态 | 表现 |
|------|------|
| idle | 缓慢旋转 + 呼吸灯 |
| thinking | 加速旋转 + 光纹闪烁 |
| happy | 暖色脉冲 + 膨胀 + 粒子扩散 |
| angry | 红光闪烁 + 震动 + 棱角锐化 |
| speaking | 面板展开 + 声波纹 |

技术：Three.js / CSS 3D，零美术资源依赖，1-2周开发。
